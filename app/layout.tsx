import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Diamat Maschinenbau GmbH - German Engineering Excellence",
  description: "Leading manufacturer of plastic machinery with 40+ years of excellence. Specializing in flat film extrusion, plastic manufacturing equipment, and recycling systems.",
  keywords: "plastic machinery, extrusion, German engineering, Diamat, recycling systems, flat film",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
